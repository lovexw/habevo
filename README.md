# HABEVO (habevo.com)

这是一个轻量的静态官网主页（使命型/理念型页面），强调 **Health × AI × Bitcoin** 的融合与面向未来的进化（EVO）。

## 本地预览

任意静态服务器即可，例如：

```bash
python3 -m http.server 8080
```

然后访问：`http://localhost:8080`。

## 结构

- `index.html`：主页面内容
- `assets/styles.css`：样式
- `assets/main.js`：少量交互（移动端菜单、年份）
