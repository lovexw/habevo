# HABFUT (habfut.com)

这是一个现代、明亮的静态官网主页，融合 **健康、AI 与比特币** 三个维度，帮助个体建立更强韧的生命系统。设计采用优雅现代风格，三个支柱各有独特的品牌颜色。

## 本地预览

任意静态服务器即可，例如：

```bash
python3 -m http.server 8080
```

然后访问：`http://localhost:8080`。

## 结构

- `index.html`：主页面内容
- `assets/styles.css`：样式
- `assets/main.js`：少量交互（移动端菜单、年份）
